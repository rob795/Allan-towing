// Backwards-compatible alias. All business config lives in /src/config/site.config.js.
// Prefer importing from "@/config/site.config" going forward.
export { BUSINESS, SITE_CONFIG } from "@/config/site.config";
export { BUSINESS as default } from "@/config/site.config";
